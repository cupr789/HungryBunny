<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="hall">
	<select id="selectResListWithTwo" parameterType="int" resultType="hall">
		select * from hall where shopNo=#{shopNo} and seatCnt=2 and hallStatus=0;
	</select>	
	
	<select id="selectResListWithFour" parameterType="int" resultType="hall">
		select * from hall where shopNo=#{shopNo} and seatCnt=4 and hallStatus=0;
	</select>
	
	<select id="selectResList" parameterType="int" resultType="hall">
		select *, count(1) as cnt from hall where shopNo=#{shopNo} and hallStatus=0 group by seatCnt;
	</select>
</mapper>  